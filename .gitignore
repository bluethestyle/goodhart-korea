# Python
__pycache__/
*.pyc

# Local working directories
tmp/
logs/
lightning_logs/

# Paper preview renders
paper/preview/
paper/figures/_preview/

# Paper fonts (114MB, downloaded from Google Fonts — KoPubWorld + Noto KR)
paper/fonts/

# Big binary / regeneratable (warehouse는 공개 데이터 + 재현 편의를 위해 트랙)
# data/warehouse.duckdb (56MB) 트랙됨 — fetch 재실행 없이 즉시 분석 가능
data/budget/raw/
data/budget/2026_b0.json

# External raw caches — all in warehouse, regeneratable via fetch scripts (see data/external/MANIFEST.md)
# data/external/SOURCES.md and MANIFEST.md remain tracked
data/external/bok_data/
data/external/data_go_kr/
data/external/kosis_data/
data/external/kosis_meta/
data/external/molit/
data/external/kosis_catalog.json
# gir/ is manual-download only — keep tracked for archive (small, 48KB)

# Exploratory metadata (regeneratable via fetch_specs.py / fetch_kodas.py / probe_*)
data/all_apis.json
data/api_specs*
data/kodas_catalog*
data/specs_summary.txt
data/cases/

# Lightning checkpoints (NeuralProphet uses pytorch-lightning by default)
lightning_logs/
.lr_find_*.ckpt

# Probe HTML/JSON caches (regeneratable)
raw/

# Root-level temp files
/통계표

# MkDocs build
site/

# Paper build
paper/main.pdf
paper/*.pdf
paper/.typst-cache/

# Secrets
.env
*.key

# OS
.DS_Store
Thumbs.db
