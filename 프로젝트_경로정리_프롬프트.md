# 프로젝트 경로·파일 구조 정리 — 작업 프롬프트 (다른 세션용)

> 이 문서는 **별도 세션**에서 재정자료분석 프로젝트의 소스코드·생성파일·경로를 정리하기 위한 작업 지시서다.
> 작성 맥락: 2026-05-30. 값 검증 중 **"12월 집행 비중 37.2%"라는 버그값이 보고서·슬라이드 여러 곳에 박혀 있던 사건**이 계기.
> 원인은 *소스 코드 → 생성 파일 → 소비처(논문/슬라이드)의 경로·계보가 뒤죽박죽*이라, 버그 스크립트가 낸 값이 나중에 수정됐는데도 *다운스트림 문서에 옛값이 그대로 남았던 것*. 단일 진실원천(single source of truth)과 값 계보(provenance)가 없다.

---

## 0. 한 줄 목표

**최종 산출물은 단 둘 — 논문(`paper/main_v2.typ`)과 슬라이드(figma SVG 기반)** 뿐이다. 나머지는 *이 둘을 만들기 위한 소스/데이터/생성물*로 명확히 계층화하고, **모든 정량값이 원천 CSV/스크립트로 추적 가능**하게 만든다.

---

## 1. 현재 상태 (2026-05-30 실측 인벤토리)

| 영역 | 파일수 | 비고 |
|---|---|---|
| `scripts/` | **185** | `h*`(분석) 51 · `redo_*` 37 · `build_*` 35 · `fetch_*` 9 + probe/eda/fig2 등. **중복·재작업(redo_) 다수** |
| `data/results/` | **94** (CSV 85) | 분석 산출 CSV — *소비처(어느 figure/문서)가 불명확* |
| `data/external/` | 8 | 외부 지표 원자료 (KOSIS·ECOS·GIR 등) |
| `paper/figures/` + `eda/` | **183** | 논문·슬라이드용 차트. `_v/`·`_preview/`·`report/`·`appendix_formulas/` 하위 혼재 |
| `figma_exports/` | 19 | **★ 현재 슬라이드 정본(SVG)** |
| `paper/재정데이터 분석 ppt/` | 70 | 스토리보드·wireframes·wireframes_draft·App-*.md |
| `paper/slides/` | **957** | **옛 HTML 덱 + node_modules — 폐기 대상** (SVG로 전환 완료) |
| `deliverables/` | 25 | moef·audit_board 등 — 용도 점검 필요 |
| `docs/` | 55 | JOURNEY.md 등 분석 기록 (mkdocs) |
| `lightning_logs/` | **161** | PyTorch Lightning 로그 — **junk, 삭제** |
| `_hier/` `_r/` `_v/` | **0 (빈 디렉터리)** | 삭제 |
| `tmp/` `logs/` | 10 / 35 | 임시·로그 — 삭제/아카이브 |

**핵심 문제**:
1. **슬라이드 시스템이 3중**으로 존재 — `paper/slides/`(옛 HTML), `figma_exports/`(현 SVG 정본), `paper/재정데이터 분석 ppt/wireframes*`(중간 와이어프레임). 정본만 남기고 정리.
2. **스크립트 185개에 `h*`→`redo_*`→`build_*` 계보가 얽힘.** 같은 분석을 여러 번 재작업(redo)한 흔적. 어느 스크립트가 *현재 쓰이는 최종본*인지 불명확 → 37.2% 버그가 살아남은 근본 원인.
3. **CSV 85개 ↔ figure ↔ 문서값의 계보 없음.** 값이 바뀌어도 어디까지 전파해야 하는지 추적 불가.
4. **고아 파일**: `slides.typ`·`analysis_report.typ` 삭제(2026-05-30) 후 `slides.pdf`·`analysis_report.pdf`가 원본 없이 남음. `main.typ/pdf`(구버전)·`main_v2_en.typ/pdf`(영문)·`02_swiss_minimal_v3.typ`(드래프트)도 용도 점검.

---

## 2. 보존 — 절대 건드리지 말 것 (allowlist)

- `paper/main_v2.typ` + `paper/main_v2.pdf` — **논문 정본**
- `paper/fonts/` — 폰트 (컴파일 의존)
- `paper/refs.bib` — 참고문헌
- `paper/figures/**` 중 **main_v2.typ이 참조하는 33개** (참조 목록은 `grep -oE 'figures/[^"]*\.(png|pdf)' paper/main_v2.typ`로 추출)
- `figma_exports/**` — **슬라이드 정본(SVG)**
- `paper/재정데이터 분석 ppt/00_스토리보드.md` + `App-*.md` + `Q&A_카드.md` — 슬라이드 내러티브·발표 자료
- `data/warehouse.duckdb` + `data/results/**.csv` + `data/external/**` — **값의 진실원천**
- `.git/` · `CLAUDE.md` · `AGENTS.md` · `README.md` · `requirements.txt` · `~/.claude/.../memory/`
- `graphify-out/` — 코드 그래프 (post-commit 훅으로 자동 갱신)

---

## 3. 정리 대상 — 분류 → 격리 → 삭제 (단계별)

**원칙: 즉시 삭제 금지. `_archive/`로 옮긴 뒤(git mv) 컴파일·정본 무결성 확인 → 다음 커밋에서 삭제.**

### 3-1. 명백한 junk (바로 삭제 가능, git 추적이면 복구됨)
- `lightning_logs/` (161) · `tmp/` (10) · 빈 디렉터리 `_hier/` `_r/` `_v/`
- `logs/` (35) — 내용 확인 후 아카이브

### 3-2. 폐기된 슬라이드 시스템
- `paper/slides/` (957) — 옛 HTML 덱 + `node_modules`. **전체 아카이브/삭제.** (단, 혹시 figma_exports가 참조하는 자산이 있는지 `paper/slides/assets/` 1회 확인)
- `paper/재정데이터 분석 ppt/wireframes/` · `wireframes_draft/` — figma로 대체됐으면 아카이브

### 3-3. 고아 산출물
- `paper/slides.pdf` · `paper/analysis_report.pdf` (원본 typ 삭제됨)
- `paper/main.typ`/`main.pdf` (구버전) · `paper/02_swiss_minimal_v3.typ` (드래프트)
- `paper/main_v2_en.typ`/`main_v2_en.pdf` — **영문판 유지 여부는 사용자 확인** (Zenodo 영문 초록과 연계 가능성)

### 3-4. 스크립트 정리 (가장 신중히)
- **목표**: `scripts/`를 *현재 쓰이는 최종 스크립트*만 남기고, `redo_*`·구버전 `h*`는 `scripts/_archive/`로.
- **절차**:
  1. main_v2.typ이 참조하는 33개 figure + figma 슬라이드가 쓰는 차트의 **생성 스크립트를 역추적** (figure 파일명 → 생성 스크립트 grep).
  2. 그 스크립트들이 읽는 CSV → 그 CSV를 만든 분석 스크립트(`h*`) — *체인의 최종본만* 활성으로 표시.
  3. 나머지(중간 redo_·probe_·미사용 h_)는 아카이브.
  4. **fig2_monthly_exec.py 같은 "수정본"이 버그 원본을 대체했는지** 확인하고 버그 원본은 아카이브 + 주석.

---

## 4. ★ 핵심 산출물 — 값 계보(provenance) 시스템

37.2% 사건 재발 방지를 위해, **논문·슬라이드의 모든 정량값이 원천으로 추적되는 매니페스트**를 만든다.

`VALUES.md` (또는 `data/PROVENANCE.md`) 작성 — 표 형식:

| 값 ID | 값 | 의미 | 원천 CSV/스크립트 | 재현 명령 | 소비처(논문§/슬라이드) |
|---|---|---|---|---|---|
| rdd_total | 1.91배 | 전체 12월 RDD 점프 | `data/results/H22_rdd_estimates.csv` (β=0.648) | `scripts/h22_*.py` | 논문 §6.3·초록, slide-23 |
| eda_dec | 12.05% | 12월 정규화 비중 | warehouse, `fig2_monthly_exec.py` (GROUP BY FSCL_YY,ACTV_CD) | 해당 SQL | 논문 §1, slide-02 |
| … | | | | | |

- **규칙**: 논문·슬라이드에 새 수치를 넣을 땐 *반드시* 이 표에 행을 추가하고 원천을 명시.
- 가능하면 **빌드 스크립트가 하드코딩 대신 CSV에서 값을 읽도록** 리팩터(예: `build_overview_figma_slides.py`의 `vals=[...,17.7,37.2]` 하드코딩 → CSV 로드). 이게 37.2% 버그의 직접 원인이었다.

---

## 5. 권장 최종 구조 (예시)

```
재정자료분석/
├─ paper/                 # 논문 정본
│  ├─ main_v2.typ · main_v2.pdf · refs.bib · fonts/
│  └─ figures/            # main_v2.typ 참조 차트만 (33개 + 부록)
├─ slides/                # 슬라이드 정본 (figma_exports/ 통합·개명)
│  ├─ svg/                # *.svg
│  └─ storyboard.md · App-*.md · Q&A.md
├─ src/                   # 활성 스크립트만 (analysis/ · figures/ · slides_build/)
│  └─ _archive/           # redo_*·구버전 (참고 보존)
├─ data/
│  ├─ warehouse.duckdb · results/*.csv · external/
│  └─ PROVENANCE.md       # ★ 값 계보
├─ docs/                  # JOURNEY 등 기록
├─ CLAUDE.md · AGENTS.md · README.md
└─ _archive/              # 폐기 슬라이드·고아 산출물 (삭제 전 격리)
```
(폴더명·한글경로는 사용자 취향 따라. 핵심은 *소스/데이터/산출물/정본의 4계층 분리* + *provenance*.)

---

## 6. 안전 절차 (필수)

1. 작업 전 **`git add -A && git commit`** 으로 현재 상태 스냅샷.
2. **새 브랜치**(`cleanup/restructure`)에서 작업.
3. 삭제는 **`_archive/`로 git mv → 정본 무결성 확인 → 다음 커밋에서 rm**.
4. 각 단계 후 **검증**: `typst compile paper/main_v2.typ` 통과 + figma_exports SVG 개수·참조 figure 33개 존재 확인.
5. **한글 경로 주의** — `paper/재정데이터 분석 ppt/` 등 공백·한글 포함 경로는 git mv 시 따옴표 필수.
6. 완료 후 **structure README** + **PROVENANCE.md** 갱신, CLAUDE.md의 경로 규약 업데이트.

---

## 7. 시작 명령 (이 프롬프트를 받은 세션이 처음 할 일)

1. `git status` + `git stash list` 로 미커밋 변경 확인 후 스냅샷 커밋.
2. 위 §1 인벤토리를 현재 시점으로 재실측 (파일수 변동 확인).
3. §3-1(junk) → §3-2(폐기 슬라이드) → §3-3(고아) → §3-4(스크립트) 순으로 단계별 진행, 각 단계 커밋.
4. §4 PROVENANCE.md 작성 (값 검증 결과 `슬라이드_수정사항.md`·논문 §1 EDA의 12.05% 사례부터 시작).
5. 사용자에게 단계별 보고.

> **주의**: 이 작업은 *구조 정리*이지 *내용/분석 변경*이 아니다. 논문 본문·슬라이드 내러티브·분석 결과는 건드리지 않는다. 값이 틀린 곳은 `슬라이드_수정사항.md`(별도)에 따라 정정하되, 그것도 *값 교체*이지 구조 변경이 아니다.
